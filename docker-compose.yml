services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: coach_studio
      POSTGRES_USER: coach
      POSTGRES_PASSWORD: devpassword
    ports:
      - "5432:5432"
    volumes:
      - coach_studio_db:/var/lib/postgresql/data

volumes:
  coach_studio_db:
